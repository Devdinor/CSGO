# CSGO
Mousewheel Jump Bind
bind mwheelup +jump;bind mwheeldown +jump;bind space +jump

LongJump bind
alias +LJ "+duck; +jump; +klook"
alias -LJ "-duck; -jump; -klook"
bind space +LJ

viewmodel
viewmodel_fov 90
